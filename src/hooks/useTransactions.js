/**
 * Hook useTransactions
 * Re-exporta do TransactionsContext para manter compatibilidade
 * com todas as telas que já importam deste caminho.
 */
export { useTransactions } from '../context/TransactionsContext';
